/**
 * Prefix for generated position codes: `CV0001`, `CV0002`… ("chức vụ").
 *
 * The seeded `STAFF` predates generation, does not match the pattern, and is
 * left as is.
 */
export const POSITION_CODE_PREFIX = 'CV';
